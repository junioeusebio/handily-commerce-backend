<Project>
  <PropertyGroup>
    <!-- Product Versioning (FE footer via GET /api/{route}/apiVersion → version). Route segment stays Api:Version. -->
    <Version>0.2.0</Version>
  </PropertyGroup>
</Project>